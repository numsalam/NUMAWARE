export type Service = {
  title: string;
  description: string;
};

export type CourseModule = {
  title: string;
  description: string;
};

export type Post = {
  title: string;
  excerpt: string;
  date: string;
};
